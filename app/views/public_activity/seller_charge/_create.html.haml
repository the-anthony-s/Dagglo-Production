- if activity.trackable
  %li(class="activity")
    %div(class="activity-container")
      %div(class="activity-icon")
        %div(class="activity-icon-main")
          = image_tag activity.trackable.user.avatar(300, 300), 'data-src': activity.trackable.user.avatar(300, 300), class: "lazy", loading: "lazy"
        %div(class="activity-icon-action activity-icon-action-create")
          = icon "far", "user-plus"
      %div(class="activity-body")
        %div(class="activity-body-desc")
          %p(class="text text-s") 
            = link_to activity.trackable.name, dashboard_seller_path, class: "link" 
            created by 
            = link_to activity.owner.full_name, account_path(activity.owner.seller_account), class: "link"
        %div(class="activity-body-time")
          %p(class="text text-xs text-secondary")
            = time_ago_in_words(activity.created_at).capitalize
            ago




-# %li(class="activities-item")
-#   %div(class="activity")
    
-#     - if activity.trackable
      
-#       %div(class="activity-container")
        
-#         %div(class="activity-icon")
-#           %div(class="activity-icon-recipient")
-#             = image_tag activity.trackable.logo(300, 300), loading: "lazy"
-#           %div(class="activity-icon-action activity-icon-action-create")
-#             = icon "fal", "plus"
        
-#         %div(class="activity-main")
-#           %div(class="activity-message")
-#             %p(class="text text-s") #{link_to activity.trackable.name, dashboard_seller_path, class: "link"} created by #{link_to activity.owner.full_name, account_path(activity.owner.seller_account), class: "link"}
-#           %div(class="activity-time")
-#             %p(class="text text-xs text-secondary") #{time_ago_in_words(activity.created_at).capitalize} ago

-#     - else
      
-#       %div(class="activity-container")
-#         %div(class="activity-icon")
-#           %div(class="activity-icon-lost")
-#             = icon "far", "ghost"
        
-#         %div(class="activity-main")
-#           %div(class="activity-message")
-#             %p(class="text text-s") Activity is not available
-#           %div(class="activity-time")
-#             %p(class="text text-xs text-secondary") #{time_ago_in_words(activity.created_at).capitalize} ago