%header.header
  .content-page
    .header-container
      .header-main
        %h1.text.text-xxxl Available categories


- @roots = Category.roots.is_active.all
- @roots.order(name: :asc).each do |root|
  %section.section
    .content-page
      .section-header
        %h1.text.text-l.text-medium #{root.name}
      .section-block
        .grid-1-3
          - root.children.order(name: :asc).each_with_index do |category, index|
            .col
              = render partial: "categories/elements/card", :locals => {:category => category, :index => index}